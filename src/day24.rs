use crate::io::read_lines;

pub(crate) fn day24() {
    let lines = read_lines("input/day24/input.txt").unwrap();
    for line in lines.into_iter().map(|l| l.unwrap()) {
        // TODO: have fun
    }
}
